# +required The configuration file schema version
schemaVersion: 1.2

# +optional Incoming connection details for the component
endpoints:
  # +required Unique name for the endpoint.
  # This name will be used when generating the managed API
  - name: order_events_ws
    # +optional Display name for the endpoint.
    displayName: Order Status Events Stream
    # +required Service section has the user service endpoint details
    service:
      # +optional Base path of the API that gets exposed via the endpoint.
      # Prefix the gateway prepends to the incoming path before forwarding:
      # ServicePath = basePath + (path after the API context). The Ballerina
      # service already answers on /orders/events, so this must contribute
      # nothing - same as the orderstatus_mcp component.
      basePath: /
      # +required Numeric port value that gets exposed via the endpoint
      port: 9091
    # +required Type of traffic that the endpoint is accepting.
    # Allowed values: REST, GraphQL, GRPC, TCP, UDP, WS.
    type: WS
    # +optional Network level visibilities of the endpoint.
    # Accepted values: Project|Organization|Public(Default).
    networkVisibilities:
      - Public
    # +optional The AsyncAPI definition, relative to the component root.
    # Without this, the endpoint is exposed as a wildcard route.
    schemaFilePath: asyncapi.yaml
